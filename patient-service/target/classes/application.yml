server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/patient_service_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: appuser
    password: AppPass123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
app:
  jwt:
    secret: supersecretkeysupersecretkeysupersecretkey
    expirationMs: 86400000
